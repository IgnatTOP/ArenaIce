# 🏒 ArenaIce - Система управления ледовой ареной

Полнофункциональная система управления ледовой ареной с современным REST API и адаптивным веб-интерфейсом.

## 📋 Описание

ArenaIce - это комплексное решение для автоматизации работы ледовой арены, включающее:
- Продажу билетов на спортивные события с интерактивной схемой мест
- Управление секциями хоккея и фигурного катания
- Систему бронирования льда для аренды
- Личные кабинеты для клиентов и тренеров
- Административную панель для управления расписанием и заявками

## 🚀 Быстрый старт

### С использованием Docker (рекомендуется)

```bash
# Запуск всех сервисов
docker-compose up -d

# Frontend: http://localhost:5173
# Backend API: http://localhost:8000
# API Docs: http://localhost:8000/api/docs/
```

### Локальная разработка

#### Backend

```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

#### Frontend

```bash
cd frontend
npm install
npm run dev
```

## 📁 Структура проекта

```
ArenaIce/
├── backend/              # Django REST API
│   ├── users/           # Аутентификация, роли (клиент, тренер, админ)
│   ├── events/          # События, билеты, схема зала
│   ├── sections/        # Секции хоккея/фигурки, расписание групп
│   ├── bookings/        # Аренда льда по слотам
│   └── core/            # Общие утилиты и валидаторы
└── frontend/            # React + TypeScript (FSD)
    └── src/
        ├── app/         # Роутинг, providers
        ├── pages/       # Страницы (home, events, profile)
        ├── widgets/     # Сложные компоненты (EventCard, SeatMap)
        ├── features/    # Фичи (auth, booking)
        ├── entities/    # Бизнес-логика (user, event, booking)
        └── shared/      # UI-kit, утилиты, API client
```

## ✨ Основные возможности

### Для посетителей
- 🎫 **Покупка билетов** - интерактивный выбор мест на схеме арены (500+ мест)
- 📅 **События** - матчи, соревнования, показательные выступления
- ⛸️ **Запись в секции** - хоккей и фигурное катание для детей и взрослых
- 👤 **Личный кабинет** - билеты, расписание тренировок, абонементы

### Для тренеров
- 📊 **Расписание групп** - просмотр своих тренировок
- 👥 **Списки учеников** - информация о группах
- 📝 **Заявки** - обработка записей в секции

### Для администраторов
- 🗓️ **Управление расписанием** - создание событий и слотов
- 🏟️ **Конфигурация зала** - настройка схемы мест (секторы, ряды)
- 📈 **Статистика** - продажи, загруженность, финансы
- ⚙️ **Управление заявками** - одобрение записей в секции

## 🛠️ Технологии

### Backend
- **Django 5** + **Django REST Framework** - REST API
- **PostgreSQL** - основная БД
- **JWT** - аутентификация (access + refresh tokens)
- **Celery** + **Redis** - асинхронные задачи и кеширование
- **drf-spectacular** - автогенерация OpenAPI документации

### Frontend
- **React 18** + **TypeScript** - UI
- **Vite** - сборка и dev-сервер
- **TanStack Query** - управление серверным состоянием
- **Zustand** - глобальный стор (auth)
- **React Router v6** - маршрутизация
- **React Hook Form** + **Zod** - формы и валидация
- **shadcn/ui** + **Tailwind CSS** - UI-компоненты
- **Framer Motion** - анимации
- **Swiper** - карусели
- **Axios** - HTTP клиент
- **FSD (Feature-Sliced Design)** - архитектура

## 📖 Документация

- **API**: http://localhost:8000/api/docs/ (Swagger/OpenAPI)
- **Admin панель**: http://localhost:8000/admin/
- **Правила для AI агентов**: см. `AGENTS.md`

## 🧪 Тестирование

```bash
# Backend
cd backend
python manage.py test

# Frontend
cd frontend
npm run lint
```

## 🔧 Полезные команды

### Backend
```bash
python manage.py makemigrations    # Создать миграции
python manage.py migrate           # Применить миграции
python manage.py createsuperuser   # Создать суперпользователя
python manage.py test              # Запустить тесты
```

### Frontend
```bash
npm run dev       # Запуск dev-сервера
npm run build     # Сборка для production
npm run preview   # Просмотр production сборки
npx eslint .      # Проверка кода
```

## 🌐 Порты

- Frontend: `5173`
- Backend: `8000`
- PostgreSQL: `5432`
- Redis: `6379`

## 📝 Лицензия

Частный проект

## 👨‍💻 Разработка

При разработке следуйте правилам из `AGENTS.md`:
- Backend: PEP 8, snake_case для переменных
- Frontend: FSD архитектура, TypeScript strict typing
- Валидация на обеих сторонах
- Язык интерфейса: русский (ru-RU)
- Часовой пояс: Europe/Moscow
